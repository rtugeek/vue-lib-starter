import type { App, Plugin } from 'vue'
import 'virtual:uno.css'
import './style/style.css'


const WidgetJsPlugin: Plugin = {
  install(app: App) {
    // app.component('WidgetEditDialog', WidgetEditDialog)
    // app.directive('no-electron', hideInElectron)
  },
}

export {

}
